{
  "name": "skillverse",
  "version": "1.0.0",
  "description": "Educational platform for Computer Science courses with integrated code compiler and AI-powered skill assessment",
  "scripts": {
    "start": "concurrently \"pnpm --prefix backend start\" \"pnpm --prefix frontend start\"",
    "install-all": "pnpm install && pnpm --prefix backend install && pnpm --prefix frontend install",
    "build": "pnpm --prefix frontend build",
    "dev": "concurrently \"pnpm --prefix backend dev\" \"pnpm --prefix frontend start\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=7.0.0"
  }
}